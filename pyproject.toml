[project]
name = "pytorch-compliance"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.3.4",
    "pytest>=8.4.2",
    # if you change this to 2.8.0, the linear test will fail. try it!
    "torch==2.9.0",
]
